using System;
using static ForaysUI.ScreenUI.StaticScreen;

namespace ForaysUI.ScreenUI{
    public static class ScreenUI{
        static void a(){
            Screen.HoldUpdates = true;
        }
    }
    //todo, is the UI here also an interface that has a standard way to receive the GameUniverse?
}
